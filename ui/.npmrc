; Project-local npm config. Points at macOS's system CA bundle so node 25's
; stricter TLS validation can verify registry.npmjs.org without falling back
; to NODE_EXTRA_CA_CERTS env var. Scoped to this directory only — doesn't
; touch your ~/.npmrc.
cafile=/etc/ssl/cert.pem
